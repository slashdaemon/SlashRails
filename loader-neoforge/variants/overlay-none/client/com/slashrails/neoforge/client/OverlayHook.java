package com.slashrails.neoforge.client;

/** MC 1.21.11+: the overlay is drawn as vanilla gizmos (compat/lines-gizmo), so there is nothing to register. */
final class OverlayHook {

    private OverlayHook() {
    }

    static void register() {
    }
}
