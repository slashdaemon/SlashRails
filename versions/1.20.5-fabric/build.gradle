plugins { id "fabric-loom" }
ext.rails = [
        compat : ["cart-legacy", "tick-wrap-legacy", "chunk-moving", "item-plain", "recipe-1205", "nbt-plain", "store-factory", "ids-ctor", "sprite-atlasfn", "tooltip-context", "shot-plain", "perms-level", "lines-vertexpose", "net-payload"],
        loader : ["model-forwarding-legacy", "overlay-events", "net-payload"],
]
apply from: "${rootDir}/gradle/fabric-band.gradle"
