plugins { id "fabric-loom" }
ext.rails = [
        compat : ["cart-legacy", "chunk-moving", "item-plain", "recipe-1205", "nbt-plain", "store-factory", "ids-ctor", "sprite-atlasfn", "tooltip-context", "shot-plain", "perms-level", "lines-vertex"],
        loader : ["model-forwarding", "overlay-events"],
]
apply from: "${rootDir}/gradle/fabric-band.gradle"
