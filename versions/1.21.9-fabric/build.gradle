plugins { id "fabric-loom" }
ext.rails = [
        compat : ["cart-behavior", "chunk-flags", "item-setid", "recipe-1212", "nbt-optional", "store-codec", "savedtype-string", "ids-factory", "sprite-atlasmanager", "tooltip-display", "shot-scale", "perms-level", "lines-buffer", "net-payload"],
        loader : ["model-blockstate", "overlay-debugmixin", "net-payload"],
        renames: ["getvalue", "ride3"],
]
apply from: "${rootDir}/gradle/fabric-band.gradle"
