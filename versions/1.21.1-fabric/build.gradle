plugins { id "fabric-loom" }
ext.rails = [compat: ["cart-legacy", "chunk-moving", "item-plain", "recipe-121", "nbt-plain", "store-factory", "ids-factory", "sprite-atlasfn"], loader: ["model-forwarding"], devtools: true]
apply from: "${rootDir}/gradle/fabric-band.gradle"
